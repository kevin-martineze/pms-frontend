# Copy to .env.local and fill in. Never commit the filled version.

# La API de NestJS (repo bookings-api) es la única que toca la base de datos.
# Este panel y el sitio del huésped son ambos clientes suyos.
NEXT_PUBLIC_API_URL="http://localhost:3000"
